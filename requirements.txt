# Core dependencies
opencv-python>=4.8      # template matching (the "image recognition")
numpy>=1.24
mss>=9.0                # screen capture (cross-platform, easy install)
pynput>=1.7             # mouse/keyboard control + global hotkeys
customtkinter>=5.2      # modern dark UI (sidebar, rounded widgets)

# Optional speed upgrade (Windows only). If installed, capture.py uses it
# automatically for much faster grabs. Safe to skip for an MVP.
# dxcam>=0.0.5

# Optional: OCR, for steps that find moving TEXT instead of a fixed image
# (the find_text_click / wait_for_text actions). Pip-only, no external binary.
# Install it only if you use text steps -- it's a ~200MB onnxruntime download.
#   pip install rapidocr-onnxruntime
# rapidocr-onnxruntime>=1.3

# Optional: ML object detector, for steps that find a CLASS of element -- e.g.
# any "button" regardless of its label/theme (find_object_click / wait_for_object).
# Runtime needs only onnxruntime; put a YOLOv8 .onnx model in models/.
#   pip install onnxruntime
# onnxruntime>=1.16
# Only needed to TRAIN your own model (not to run macros):
#   pip install ultralytics
