Anchor images for the "example" macro go here.

The example macro looks for `target_button.png` in this folder. Create it with:

    python grab_anchor.py example/target_button

Then drag a box around any button on your screen and press ENTER.
Run the macro with:

    python main.py
